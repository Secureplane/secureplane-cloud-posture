apiVersion: v1
kind: Service
metadata:
  name: wiz-console
  namespace: wiz-system
spec:
  type: ClusterIP
  ports:
  - port: 443
    targetPort: 443
    protocol: TCP
  selector:
    app: wiz-console